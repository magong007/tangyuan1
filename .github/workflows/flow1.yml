# This is a basic workflow to help you get started with Actions  

name: flow1 

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the "master" branch
  push:
    branches: [ "*" ]
  pull_request:
    branches: [ "*" ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4

      # Runs a single command using the runners shell
      - name: Run a one-line script
        run: echo Hello, world!
        

      # Runs a set of commands using the runners shell
      - name: Run a multi-line script
        run: |
          echo Add other actions to build,
          echo test, and deploy your project.

      # - name: Get Pokemon name
      #   uses: ./ # Uses an action in the root directory
      #   id: pokemon
      #   with:
      #     pokemon_id: 150

      # - name: Get the pokemon
      #   run: echo "${{ steps.pokemon.outputs.pokemon_name }} attack"




      - name: Get Change Status from ServiceNow
        run: |
          CHANGE_SYS_ID="CHG0000076"  # Replace with actual sys_id or fetch dynamically
          RESPONSE=$(curl -s -u "${{ env.SN_USERNAME }}:${{ env.SN_PASSWORD }}"  \
          "https://dev323747.service-now.com/api/now/table/change_request/${CHANGE_SYS_ID}?sysparm_fields=number,state"   \
          -H "Accept: application/json")
           # "https://${{ secrets.SN_INSTANCE_URL }}/api/now/table/change_request/${CHANGE_SYS_ID}?sysparm_fields=number,state"  "https://dev323747.service-now.com/api/now/table/change_request/${CHANGE_SYS_ID}?sysparm_fields=number,state"   -H "Accept: application/json")
                    #    "https://dev323747.service-now.com/api/now/table/change_request/${CHANGE_SYS_ID}?sysparm_fields=number,state" \
             # -H "Accept: application/json")
          echo "response:$RESPONSE"  
          CHANGE_NUMBER=$(echo "$RESPONSE" | jq -r '.result.number')
          CHANGE_STATE=$(echo "$RESPONSE" | jq -r '.result.state')
          echo "Change Number: $CHANGE_NUMBER, Status: $CHANGE_STATE"
        env:
          # SN_USERNAME: ${{ secrets.SN_USERNAME }}
          # SN_PASSWORD: ${{ secrets.SN_PASSWORD }}
          # SN_INSTANCE_URL: ${{ secrets.SN_INSTANCE_URL }}
          SN_USERNAME: admin
          SN_PASSWORD: =Nec4hmHU6=P
          SN_INSTANCE_URL: https://dev323747.service-now.com

